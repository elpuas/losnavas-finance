# Change Log

## Task summary

Connected the Income screen to real Supabase data with period filtering.

## Files created or modified

- `/src/app/screens/Income.tsx`
- `.context/2026-03-20-1116-income-wiring.md`

## Description of changes

- Replaced mock income data with Supabase queries against the `income` table
- Added a period selector using shared selected-period state
- Filtered all income rows by the selected period
- Recalculated total income and per-user totals from the filtered rows only
- Rendered the income list from real data and added a clean empty state for periods with no income
- Kept the existing UI structure and visual layout intact

## Assumptions

- `user_name` in the database may be null and only `Alfredo` or `Cata` should count toward per-user totals
- `income_date` may be null and should fall back to today for display only

## Next steps

- Verify income refreshes after new income transactions are added
- Confirm period selector stays in sync with Dashboard and Expenses
